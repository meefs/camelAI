{
  "name": "chiridion-starter-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "sh -lc 'test -n \"${CHIRIDION_BASE_URL:-}\" || (echo \"CHIRIDION_BASE_URL is required\" >&2; exit 1); CLOUDFLARE_API_BASE_URL=\"${CHIRIDION_BASE_URL%/}/client/v4\" wrangler deploy --dispatch-namespace ignored --name ignored'"
  }
}
