{
  "name": "chiridion-starter-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "sh -lc 'test -n \"${CLOUDFLARE_API_BASE_URL:-}\" || (echo \"CLOUDFLARE_API_BASE_URL is required\" >&2; exit 1); wrangler deploy --dispatch-namespace ignored --name ignored'"
  }
}
